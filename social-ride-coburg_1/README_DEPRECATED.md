# DEPRECATED: duplicate upload folder

Dieses Verzeichnis ist eine Duplikat-/Upload-Kopie des Projekts und wurde im Branch `chore/cleanup-repo` als redundant markiert.

Empfohlene Aktion:
1. Vergleiche bei Bedarf Dateien mit dem Root-`src/` (falls du Änderungen retten willst).
2. Wenn keine relevanten Änderungen vorhanden sind: lösche dieses Verzeichnis vollständig (git rm -r social-ride-coburg_1) und committe den Löschvorgang.

Die tatsächliche Löschung erfolgt nur nach deiner ausdrücklichen Bestätigung.